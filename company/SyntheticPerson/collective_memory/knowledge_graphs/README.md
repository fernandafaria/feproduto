# Knowledge Graphs

Cross-cutting structured knowledge:

- `entities/` — canonical entities (companies, products, regulations, places)
- `relations/` — typed relations between entities
- `events/` — time-stamped events as graph nodes
- `schemas/` — graph schemas, versioned

The graph is read by every system that needs structural reasoning over
shared knowledge.
