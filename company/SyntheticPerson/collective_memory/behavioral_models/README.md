# Behavioral Models

The cores of Synthetic Persons and Synthetic Societies:

- `archetypes/` — non-personal archetype models
- `populations/` — Society models
- `segments/` — derived segment models
- `traits/` — trait dictionaries
- `validity/` — population validity reports

Heavy compartmentalization for any model derived from consented persons.
