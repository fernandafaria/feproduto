# Simulations

All simulation runs:

- `runs/` — every dispatched run with full replay info
- `calibration/` — per-engine calibration histories
- `replays/` — saved replay artifacts
- `templates/` — reusable scenario templates
