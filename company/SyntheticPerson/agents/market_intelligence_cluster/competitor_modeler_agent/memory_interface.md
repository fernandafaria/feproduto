# Memory Interface — competitor_modeler_agent

Reads: `market_memory/signals/`, `market_memory/competitors/{id}/`. Writes:
`market_memory/competitors/{id}/model_v{n}.json`,
`market_memory/competitors/{id}/predictions/{ts}.json`. Provenance complete.
