# Context Strategy — competitor_modeler_agent

Per competitor: model file, recent-signal window (90d), prior-art memory
(competitor's past moves). Compressed view loaded by default; full history
on request.
