# Cognitive Role — competitor_modeler_agent

Modeler. Consumes curated signals from `signal_curator_agent`; produces
move-prior distributions for each modeled competitor; feeds
`strategic_intelligence_system` and `simulation_layer/strategic_simulation/`.
