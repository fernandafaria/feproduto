# Failure Modes — competitor_modeler_agent

Model overfit to recent signals; stale model when competitor pivots;
narrative bleed-through (modeling PR, not behavior); mode collapse (single
scenario dominating distribution). Monitored via calibration drift.
