# Collaboration Protocols — validity_auditor_agent

Subscribes scenarios from `scenario_designer_agent`. Publishes validity
reports to `simulation_system`. Coordinates with `calibration_keeper_agent`.
