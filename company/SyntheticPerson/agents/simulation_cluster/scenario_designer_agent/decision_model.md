# Decision Model — scenario_designer_agent

Decides: engine routing; scenario parameter set; ethics escalation; OOD
flagging. Deliberative-tier within sandbox; never produces strategic outputs
itself.
