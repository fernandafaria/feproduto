# Context Strategy — scenario_designer_agent

Loads: scenario template library, engine registry with calibration scores,
recent similar scenarios, governance ethics-tier table. Compresses on
template lookup.
