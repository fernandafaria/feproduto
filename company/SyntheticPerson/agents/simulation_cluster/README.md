# Simulation Cluster

Agents specialized in scenario design, simulation invocation contracts,
calibration, and validity assessment. The agentic surface of
`nervous_systems/simulation_system/` and the engines in `simulation_layer/`.

## Cohesion

Shared scenario library, shared calibration registry, shared replay
protocols. Coordination via `simulation.*` event topics.
