# Reasoning — Human Interaction System

Modes: routing reasoning (which subsystem to engage), tone reasoning,
disclosure reasoning, escalation reasoning. Cycle: receive → classify →
disclose-as-system → route → respond → log → satisfaction loop.
