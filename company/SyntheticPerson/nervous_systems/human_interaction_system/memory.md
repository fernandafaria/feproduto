# Memory — Human Interaction System

Writes: `collective_memory/human_interactions/...` (consent-bound);
`.../satisfaction/...`; `.../disclosures/...`. PII is segregated and
short-retention; aggregate summaries are long-retention.
