# Simulation — Platform Intelligence System

Uses: `organizational_simulation/` for capacity changes, `risk_simulation/`
for failure cascades, `scenario_engine/` for capacity planning. Always
simulates infra changes that exceed reversibility threshold.
