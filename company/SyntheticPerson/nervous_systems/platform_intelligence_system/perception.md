# Perception — Platform Intelligence System

Senses: latency, throughput, error rates, cost per inference, model drift,
data drift, capacity headroom, security telemetry, dependency health.

Sensors deployed at every engine boundary in `simulation_layer/`, every
agent cluster, every memory store.
