# Interfaces — Adaptive Coordination System

Pub: coord.* events. Sub: intent streams, contradictions, subscription
events. API: `/coord/topology`, `/coord/conflicts`, `/coord/health`,
`POST /coord/rebind {subscription_diff}` (governance gate).
