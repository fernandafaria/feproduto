# Simulation — Learning System

Every architectural diff is simulated against `organizational_simulation/`
before proposal. Sensitivity tables are mandatory. Out-of-distribution
proposals require human review even at lower stakes.
