# Interfaces — Learning System

Pub: learning.* events. Sub: outcomes, failures, governance events,
ocs.topology_proposal, human evolution input. API:
`/learning/proposals`, `/learning/evolutions`, `/learning/calibrations`,
`POST /learning/propose {diff, evidence, rollback}` (governance-gated).
