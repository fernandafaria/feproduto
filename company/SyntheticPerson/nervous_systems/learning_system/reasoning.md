# Reasoning — Learning System

Modes: pattern reasoning (when to promote a pattern), retirement reasoning
(when to demote), evolution reasoning (proposing architectural diffs),
calibration reasoning. Cycle: scan → propose → simulate → ratify → roll out
→ measure → fold back.
