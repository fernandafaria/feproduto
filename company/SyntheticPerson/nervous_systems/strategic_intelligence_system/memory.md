# Memory — Strategic Intelligence System

Writes: `collective_memory/strategic_context/theses/`, `.../scenarios/`,
`.../narratives/`, `.../decisions/`. Reads broadly across the org.

Retention: theses and decisions permanent; scenarios versioned permanent;
state vectors compressed daily.
