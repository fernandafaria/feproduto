# Memory — Organizational Memory System

OMS does not have a memory of its own outside `collective_memory/`. It manages
all stores and writes its own meta-records to `meta/memory/...`: health
reports, audit trails, contradiction registers, compression journals.
