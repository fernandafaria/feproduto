# Intelligence Flows — Governance System

```
request ──► classify ──► policy graph ──► within policy?
                                          ├── yes ──► authorize ──► log
                                          └── no  ──► escalate to human ──► await ──► record
```

Cross-system: gates every material decision; co-signs constitutional changes
with human layer; advises learning system on safe evolution paths.
