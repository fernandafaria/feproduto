# Perception — Governance System

Senses: every decision request, every memory write at material tier, every
simulation invocation, every escalation, every authority assertion, every
external-facing communication.
